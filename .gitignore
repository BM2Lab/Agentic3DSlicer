# 3D Slicer installation (large binary, not part of source)
Slicer-*/

# Project data (screenshots, volumes, outputs — not tracked)
data/

# Local Slicer source copy (for Slicer; not part of this repo)
source_code/

# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.venv/
venv/
env/
sat-env/
*.egg-info/
dist/
build/

# OS
.DS_Store
Thumbs.db
*~

# IDE
.vscode/
*.swp
*.swo

# Generated database (rebuilt from index.json)
tools/tools.db

# Segmentation module — large runtime dependencies (not source code)
# Python virtual environment for SAT inference (7+ GB)
modules/Segmentation/sat-env/
# SAT third-party repo — cloned separately, has its own git history
modules/Segmentation/SAT/
# Model checkpoints — downloaded from HuggingFace, not versioned here
modules/Segmentation/checkpoints/

# Medical image files (large binary volumes and masks)
*.nii
*.nii.gz
*.dcm
*.mha
*.mhd
*.nrrd

# ML model weight files
*.pth
*.pt
*.safetensors
*.bin
